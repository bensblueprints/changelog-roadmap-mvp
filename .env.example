# Server
PORT=5311
ADMIN_PASSWORD=admin
# Where the SQLite database lives (defaults to ./data)
# DATA_DIR=./data

# Optional: BYO SMTP for "new post" subscriber emails.
# You can also configure this in the admin Settings UI (UI values win).
# SMTP_HOST=smtp.postmarkapp.com
# SMTP_PORT=587
# SMTP_SECURE=0
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="YourApp Updates" <updates@yourapp.com>
